//------------------------LOGICAL OPERATORS---------------------

/**
    == : eşit mi
    && : and
    || : or
    !  : not
    != : eşit değil mi
    >  : büyük mü
    >  : küçük mü
    >= : büyük eşit mi
    <= : küçük eşit mi
*/
    //and
    // Your age is greater than 18 AND you have 3000 TL AND you are healty

    let age = 30;
    let money = 4200;
    let healty = true;
    let hakSayisi = 3;

   //  console.log(age>18 && money>3000 && healty==true);

   //or
   //  console.log(5>3 || 2<1);

   //not
   // console.log( !(5>3) );

   // 
   // console.log( (money>3000 && hakSayisi>1) || (yas>18 && healty) );

   //!=
   console.log(age!=18);
   console.log(age!=30);
 
   //<=
   console.log(12<=13);
   // 12<12 || 12==12

   //>=
   console.log(15>=13);
   // 15>13 || 15==13
